import Image from 'next/image'
import React from 'react'
import Header from '../Header'

export default function Goal() {
  return (
    <main className='flex h-screen flex-col w-full gap-5'>
      {/* <Header title='Goal' length={0}/> */}
      To be added
    </main>
  )
}
